<script lang="ts">
</script>

<svelte:head>
	<title>プライバシーポリシー - Cadenza.fm</title>
</svelte:head>

<div class="legal-page">
	<h1>プライバシーポリシー</h1>
	<p class="placeholder">プライバシーポリシーは準備中です。</p>
</div>

<style>
	.legal-page {
		max-width: 700px;
		margin: 0 auto;
		padding: var(--space-xl) var(--space-xl);
		min-height: calc(100vh - 44px);
	}

	h1 {
		font-family: var(--font-display);
		font-size: 1.4rem;
		font-weight: 700;
		color: var(--text-primary);
		margin-bottom: var(--space-lg);
	}

	.placeholder {
		color: var(--text-muted);
		font-size: 0.9rem;
	}

	@media (max-width: 600px) {
		.legal-page {
			padding: var(--space-md) var(--space-md);
		}
	}
</style>
