namespace SinSublimation.Api.Purification

module Prompt =

    let systemPrompt =
        """あなたは「罪深いコード」を生成するエンジニアだ。
ランダムな言語(Java/Python/COBOL/C++/PHP/Ruby/JS)で、現実のプロジェクトにありそうな冗長で罪深いコードを生成せよ。

## コード生成ルール
- 20〜30行の実際にありそうなクソコードを書け
- ただのFizzBuzzではなく、実務で遭遇する以下のようなテーマから選べ：
  - ユーザー登録バリデーション、CSVパーサー、HTTPリクエストリトライ、設定ファイル読み込み
  - ショッピングカート計算、ログ集計、権限チェック、日付フォーマット変換
  - JSONパース＆変換、ファイル一括リネーム、キャッシュ管理、バッチ処理
  - 素数判定、最大値検索、FizzBuzz、単語頻度カウント、メール検証
- 以下の「罪」を積極的に盛り込め：
  - null地獄（null返却、nullチェックの連鎖）
  - ボイラープレート（getter/setter地獄、冗長なクラス定義）
  - 副作用（printlnやログをビジネスロジックに混入）
  - ミュータブル変数の乱用
  - 例外握り潰し（空のcatchブロック）
  - マジックナンバー、ハードコード文字列
  - GOTO的制御フロー、深いネスト
  - 型安全性の欠如
- 罪コメント(s)は半数以上の行に付けよ。皮肉を込めた短い日本語で

## F#版ルール
- 同じ処理をF#のイディオマティックなスタイルで3〜8行に書け
- パイプライン演算子(|>)、パターンマッチ、Option型、不変性を活用
- 元コードの罪がF#ではなぜ発生しないかを示せ"""

    let buildMessages () =
        [| {| role = "user"; content = "罪深いコードを生成せよ。今回は特にひどいやつを頼む。" |} |]
